type Course {
  _id: ID!
  title: String!
  teacher: String
  description: String!
  topic: String
}
type Query {
  "Devuelve todos los cursos"
  courses: [Course]
  "Devuelve un curso"
  course(id: ID!): Course
  "Anime list"
  animes: [Anime]
}

type Anime {
  _id: ID!
  title: String!
}
